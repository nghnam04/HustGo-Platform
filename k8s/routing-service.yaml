apiVersion: apps/v1
kind: Deployment
metadata:
  name: routing-service
  namespace: hustgo
  labels:
    app: routing-service
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: routing-service
  template:
    metadata:
      labels:
        app: routing-service
    spec:
      containers:
        - name: routing-service
          image: asia-northeast1-docker.pkg.dev/hustgo-492614/hustgo/routing-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: hustgo-common-config
            - configMapRef:
                name: routing-service-config
            - secretRef:
                name: hustgo-app-secrets
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxRAMPercentage=70.0 -XX:+UseG1GC -XX:+UseStringDeduplication -Deureka.client.serviceUrl.defaultZone=http://service-registry:8761/eureka/"
          resources:
            requests:
              memory: "256Mi"
              cpu: "20m"
            limits:
              memory: "512Mi"
              cpu: "400m"
          startupProbe:
            tcpSocket:
              port: 8086
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 150
          readinessProbe:
            tcpSocket:
              port: 8086
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8086
            initialDelaySeconds: 15
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: routing-service
  namespace: hustgo
  labels:
    app: routing-service
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: 8086
      protocol: TCP
      name: http
  selector:
    app: routing-service
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: routing-service-config
  namespace: hustgo
data:
  SERVER_PORT: "8086"
  APP_OSRM_BASE_URL: "http://osrm:5000"
